# Auto-Trader configuration.
#
# The platform runs fully without any of these — it falls back to synthetic
# market data and a simulated paper broker. Set these only when you want real
# market data and/or live trading via Alpaca (https://alpaca.markets).

# Alpaca API credentials (from your Alpaca dashboard).
ALPACA_KEY_ID=
ALPACA_SECRET_KEY=

# Trading endpoint. Use the PAPER endpoint until you have proven your setup.
#   Paper: https://paper-api.alpaca.markets
#   Live:  https://api.alpaca.markets
ALPACA_BASE_URL=https://paper-api.alpaca.markets

# Optional: enables the AI analyst in the self-improvement engine ("AI Lab").
# Without it, the platform still self-improves via the deterministic
# walk-forward optimizer — the Claude analyst just adds code/trade review.
ANTHROPIC_API_KEY=

# Optional: push alerts (fills, kill-switch trips, engine stalls) to your
# phone via a Telegram bot. Create a bot with @BotFather to get the token;
# message your bot once, then GET /getUpdates to find your chat id.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ---------------------------------------------------------------------------
# OANDA — spot FX only. Alpaca does not offer currencies.
#
# FX is by far the cheapest thing this platform can trade: a EUR/USD round
# trip costs about 0.009% against crypto's 0.600%. Set these to get real FX
# bars and, in live mode, real FX orders.
#
# The account id looks like 001-004-1234567-001 and is shown in the OANDA
# web dashboard under "Manage Funds" / account details.
# ---------------------------------------------------------------------------
OANDA_API_TOKEN=
OANDA_ACCOUNT_ID=

# PRACTICE by default, deliberately. Getting this the wrong way round is the
# one configuration mistake here that costs real money, so you have to opt in
# to the live host explicitly.
#   Practice: https://api-fxpractice.oanda.com
#   Live:     https://api-fxtrade.oanda.com
OANDA_BASE_URL=https://api-fxpractice.oanda.com
